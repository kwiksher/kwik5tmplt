local M = {
  name       = "group-new",
  members   = {},
  properties = {
    alpha = NIL,
    xScale = NIL,
    yScale = NIL,
    rotation = NIL,
    isLuaTable = false,
    x=nil,
    y=nil
  }
}

return M